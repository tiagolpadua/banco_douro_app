import 'package:flutter/material.dart';

class AppColors {
  // Cores primarias
  static const Color primary = Color(0xFF0A1F3D);
  static const Color primaryLight = Color(0xFF1B4F72);
  static const Color primaryDark = Color(0xFF071529);

  // Cores de destaque (mantendo o laranja original do app)
  static const Color accent = Color(0xFFFFA902);
  static const Color accentLight = Color(0xFFFFEDC2);

  // Cores de fundo
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFECF0F1);

  // Cores de texto
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Cores semanticas
  static const Color positive = Color(0xFF27AE60);
  static const Color negative = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);
  static const Color info = Color(0xFF3498DB);
}
